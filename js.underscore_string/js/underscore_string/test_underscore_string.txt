How to use?
===========


You can import ``underscore_string`` from ``js.underscore_string`` and ``need`` it where you want
these resources to be included on a page::

  >>> from js.underscore_string import underscore_string
  >>> underscore_string.need()
